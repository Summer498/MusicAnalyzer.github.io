export type IBeat = any;

/*
import { FunctionBase } from "../General/FunctionBase";

export abstract class IBeat extends FunctionBase {
    abstract new(): IBeat
    abstract get BarEnterTrapName(): string
    abstract get BarLeaveTrapName(): string
    abstract get BeatEnterTrapName(): string
    abstract get BeatLeaveTrapName(): string
}
*/
